interface:
  display_name: "TGC Book/Rulebook Workflows"
  short_description: "Rulebook and book component guidance with parity-safe sequencing"
  default_prompt: "Use $tgc-book-rulebook-workflows to create or import rulebooks with correct page parity and binding-safe layout checks."

dependencies:
  tools:
    - type: "mcp"
      value: "thegamecrafter"
      description: "The Game Crafter MCP server"

policy:
  allow_implicit_invocation: true
