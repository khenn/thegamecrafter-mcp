interface:
  display_name: "TGC Component Preflight"
  short_description: "Validate component requests before TGC mutations"
  default_prompt: "Use $tgc-component-preflight to validate component options and required assets before creating or updating components."

dependencies:
  tools:
    - type: "mcp"
      value: "thegamecrafter"
      description: "The Game Crafter MCP server"

policy:
  allow_implicit_invocation: true
