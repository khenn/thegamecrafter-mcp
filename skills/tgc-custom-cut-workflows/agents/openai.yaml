interface:
  display_name: "TGC Custom-Cut Workflows"
  short_description: "Choose and preflight TGC punchouts, stickers, dials, and dual-layer boards"
  default_prompt: "Use $tgc-custom-cut-workflows to recommend, validate, and update TGC custom-cut punchouts, stickers, dials, and dual-layer boards with geometry-aware checks."

dependencies:
  tools:
    - type: "mcp"
      value: "thegamecrafter"
      description: "The Game Crafter MCP server"

policy:
  allow_implicit_invocation: true
