interface:
  display_name: "TGC Image Preflight/Fit"
  short_description: "Image layout safety checks for cut/bleed/binding fit"
  default_prompt: "Use $tgc-image-preflight-fit to preflight and correct artwork placement before uploading to TGC components."

dependencies:
  tools:
    - type: "mcp"
      value: "thegamecrafter"
      description: "The Game Crafter MCP server"

policy:
  allow_implicit_invocation: true
