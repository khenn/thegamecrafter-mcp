interface:
  display_name: "TGC Parts and Dice Workflows"
  short_description: "Choose and preflight acrylics, printed dice, meeples, and play money"
  default_prompt: "Use $tgc-parts-dice-workflows to recommend, validate, and update TGC acrylic parts, printed dice, printed meeples, and play money with side-completeness and orientation checks."

dependencies:
  tools:
    - type: "mcp"
      value: "thegamecrafter"
      description: "The Game Crafter MCP server"

policy:
  allow_implicit_invocation: true
